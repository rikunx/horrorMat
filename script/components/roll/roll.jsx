import React from 'react';
import PropTypes from 'prop-types';

import Icon from '@material-ui/core/Icon';

const Roll = ({ baseRoll, clues, abilities, inventory, phase, results }) => <div id="roll-pad">{baseRoll}</div>;

Roll.propTypes = {};

export default Roll;
